# Karvalho (monorepo)

Root canonico de todos os projetos Karvalho (SPEC-KARVALHO-001).
Cada projeto vive em subpasta propria. Infra MCP fica fora (K:\MCP, repo proprio).
Historico das origens: bundles em _archive/ (fora do git).
Dados grandes (sqlite, datasets, pcap) ficam fora do git - ver scripts de build de cada projeto.
